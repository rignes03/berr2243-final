# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy Node.js app to Azure Web App - benr2423-rignes-v2

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'

      - name: npm install, build, and test
        run: |
          npm install
          npm run build --if-present
          npm run test --if-present

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: .

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write #This is required for requesting the JWT
      contents: read #This is required for actions/checkout

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: node-app
      
      - name: Login to Azure
        uses: azure/login@v2
        with:
<<<<<<< HEAD
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_EDB3BC9DA93149D99C1BBCC30870C9B4 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_AFA38325DEA440F684AD7363FAA2EAAA }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_DEBBD077C0A44E16A861120FE6984DE1 }}
=======
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_7B820DC7C74A436AB8488548A9CF3C37 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_B4504F3DEC64454991131E2E012CEE4D }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_F6125D1DCE1D402295613A1E659EE805 }}
>>>>>>> e51037b (Add or update the Azure App Service build and deployment workflow config)

      - name: 'Deploy to Azure Web App'
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'benr2423-rignes-v2'
          slot-name: 'Production'
          package: .
          